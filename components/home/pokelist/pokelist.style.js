import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    width: '100%'
  },
  card: {
    flex: 1,
    alignItems: 'center',
    width: 180,
    height: 180,
    borderWidth: 2,
    borderColor: '#ddd',
    borderRadius: 5,
  },
  cardImage: {
    width: 150,
    height: 150,
  }
});

export default styles;
